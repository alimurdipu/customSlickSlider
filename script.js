$(document).ready(function () {
  $(".slider").slick({
    //   setting-name: setting-value
    // fade: true,
    // cssEase: 'linear',

    // centerMode: true,
    // centerPadding: "60px",
    // slidesToShow: 1,
    slidesToShow: 3,
    slidesToScroll: 1,

    // autoplay: false,
    // autoplayspeed: 100,
    arrows: true,
    prevArrow: ".back",
    nextArrow: ".next",
    dots: true,
    // slidesToShow: 3,
    // slidesToScroll: 1,
  });
});
